#include<stdio.h>
#include<sys/types.h>

void main(){
	char b[] = "sir mvit        ";
	char b2[]= "dept of cse     ";
	
	FILE *fp;
	fp=fopen("demo","w");
	fprintf(fp,b);
	fseek(fp,48,0);
	fprintf(fp,b2);
	fclose(fp);
}
